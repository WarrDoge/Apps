import sqlite3
from Apps import *
from Webs import *
###################################################################
db1 = sqlite3.connect(app1 + ".db")
sql1 = db1.cursor()

sql1.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT, 
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db1.commit()
db1.close()
###################################################################
db2 = sqlite3.connect(app2 + ".db")
sql2 = db2.cursor()

sql2.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db2.commit()
db2.close()
###################################################################
db3 = sqlite3.connect(app3 + ".db")
sql3 = db3.cursor()

sql3.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db3.commit()
db3.close()
###################################################################
db4 = sqlite3.connect(app4 + ".db")
sql4 = db4.cursor()

sql4.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db4.commit()
db4.close()
###################################################################
db5 = sqlite3.connect(app5 + ".db")
sql5 = db5.cursor()

sql5.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db5.commit()
db5.close()
###################################################################
db6 = sqlite3.connect(app6 + ".db")
sql6 = db6.cursor()

sql6.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db6.commit()
db6.close()
###################################################################
db7 = sqlite3.connect(app7 + ".db")
sql7 = db7.cursor()

sql7.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db7.commit()
db7.close()
###################################################################
db8 = sqlite3.connect(app8 + ".db")
sql8 = db8.cursor()

sql8.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db8.commit()
db8.close()
###################################################################
db9 = sqlite3.connect(app9 + ".db")
sql9 = db9.cursor()

sql9.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db9.commit()
db9.close()
###################################################################
db10 = sqlite3.connect(app10 + ".db")
sql10 = db10.cursor()

sql10.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db10.commit()
db10.close()
###################################################################
db11 = sqlite3.connect(app11 + ".db")
sql11 = db11.cursor()

sql11.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db11.commit()
db11.close()
###################################################################
db12 = sqlite3.connect(app12 + ".db")
sql12 = db12.cursor()

sql12.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db12.commit()
db12.close()
###################################################################
db13 = sqlite3.connect(app13 + ".db")
sql13 = db13.cursor()

sql13.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db13.commit()
db13.close()
###################################################################
db14 = sqlite3.connect(app14 + ".db")
sql14 = db14.cursor()

sql14.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db14.commit()
db14.close()
###################################################################
db15 = sqlite3.connect(app15 + ".db")
sql15 = db15.cursor()

sql15.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db15.commit()
db15.close()
###################################################################
db16 = sqlite3.connect(app16 + ".db")
sql16 = db16.cursor()

sql16.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db16.commit()
db16.close()
###################################################################
db17 = sqlite3.connect(app17 + ".db")
sql17 = db17.cursor()

sql17.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db17.commit()
db17.close()
###################################################################
db18 = sqlite3.connect(app18 + ".db")
sql18 = db18.cursor()

sql18.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db18.commit()
db18.close()
###################################################################
db19 = sqlite3.connect(app19 + ".db")
sql19 = db19.cursor()

sql19.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db19.commit()
db19.close()
###################################################################
db20 = sqlite3.connect(app20 + ".db")
sql20 = db20.cursor()

sql20.execute("""CREATE TABLE IF NOT EXISTS webs (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
	WebAnelin INTEGER, 
	WebDem INTEGER, 
	WebMoskow INTEGER, 
	WebSecretVictory INTEGER, 
	WebGraf INTEGER, 
	WebChicago INTEGER, 
	WebLissabon INTEGER, 
	WebDubai INTEGER, 
	WebIceman INTEGER,
	WebLemon INTEGER,
	WebTenerife INTEGER
	)""")

db20.commit()
db20.close()
###################################################################